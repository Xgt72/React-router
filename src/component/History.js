import React, { Component } from "react";

class History extends Component {
    render() {
        const styles = {
            padding: "1rem",
            margin: "2rem",
            fontSize: "1.5rem"
        };
        return (
            <p style={styles}>Notre histoire.</p>
        );
    }
}

export default History;